apiVersion: apps/v1
kind: Deployment
metadata:
  name: item-aggregator
  labels:
    app: item-aggregator
spec:
  template:
    metadata:
      labels:
        app: item-aggregator
    spec:
      containers:
        imagePullPolicy: Always
        ports:
        - containerPort: 8080
      

